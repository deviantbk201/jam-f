import styled from "styled-components";

export const StyledSmoothSldierImage = styled.figure`
  padding: 1rem 4rem;

  box-shadow: 0rem 2rem 4rem rgba(0, 0, 0, 0);

  & .slide {
    transform: scale(0.7);
  }
`;
